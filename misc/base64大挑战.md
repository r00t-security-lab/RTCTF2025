## base64大挑战

### 解题思路

提示了看库源代码，就看有什么可以利用的地方吗。

那么发现base64的python库中注释了只会检测开头的部分，到开始出现等于号结束。（这里没有进行输入过滤只允许base64字符导致还有其他解法）；同时经过测试，linux的base64会解码全部内容。

那么我们直接一手构造前面的字符串满足r00t2025{}，后面的字符串`;`开头用于分割，然后执行命令即可。

`cjAwdDIwMjV7fQ==O2xzIC1hbA==`就可以找flag了。